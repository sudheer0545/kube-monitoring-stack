---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
    ## repository is already supplied if prometheus-stack 
    ## resources have been deployed to the cluster
#  - repository.yaml

  - release.yaml